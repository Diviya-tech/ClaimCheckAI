# Test inputs

Drop sample inputs here for manual/integration testing:

- `*.txt` — pasted social-media posts, blog excerpts, transcripts
- `*.png` / `*.jpg` — screenshots (week 3, vision input)
- `urls.txt` — one article URL per line

These are fixtures for exercising the pipeline end-to-end; the offline unit
tests in `tests/` do not depend on them.
