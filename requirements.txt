# ClaimCheck AI — Python dependencies
# Pin major versions loosely; lock exact versions later with a lockfile.

# --- LLM providers (accessed only via config/providers.py abstraction) ---
anthropic>=0.40.0          # Claude API SDK (primary provider)
openai>=1.0.0              # Optional second provider, wired in later

# --- Input normalization ---
trafilatura>=1.8.0         # URL article body extraction
yt-dlp>=2024.1.0           # Video download (week 4)
openai-whisper>=20231117   # Local speech-to-text for video audio (week 4)
ffmpeg-python>=0.2.0       # Audio extraction + key-frame grabbing (week 4)
# NOTE: the ffmpeg *binary* must also be installed on the system (not pip):
#   Windows: winget install ffmpeg   |   macOS: brew install ffmpeg   |   Linux: apt install ffmpeg

# --- Evidence retrieval (weeks 5-6) ---
llama-index>=0.10.0        # RAG orchestration
qdrant-client>=1.7.0       # Vector DB client
biopython>=1.83            # PubMed API access (Bio.Entrez)
tavily-python>=0.3.0       # Supplementary web search

# --- Data validation & config ---
pydantic>=2.5.0            # Schemas: Claim, AtomicFact, Evidence, Verdict, Dossier
python-dotenv>=1.0.0       # Load API keys from .env

# --- HTTP API (weeks 9-10) ---
fastapi>=0.110.0           # HTTP interface over the pipeline (api/server.py)
uvicorn[standard]>=0.29.0  # ASGI server: uvicorn api.server:app --reload
python-multipart>=0.0.9    # Multipart/form-data parsing (file uploads)

# --- Testing ---
pytest>=8.0.0
httpx>=0.27.0             # Required by FastAPI's TestClient (offline API tests)
